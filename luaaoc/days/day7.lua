local utils = require("utils")

---@alias Operator '+' | '*'

local load_data = function()
  local lines = utils.read_lines_from(7)

  ---@type table<int, int[]>
  local res = {}

  for _, line in ipairs(lines) do
  end
end
